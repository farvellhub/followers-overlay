declare type Config = {
    TIME_UPDATE: number,
    NUM_FOLLOWERS: string
}

// Personalizar variables
const config: Config = {
    "TIME_UPDATE": 2400000,
    "NUM_FOLLOWERS": "30"
}

export default config;