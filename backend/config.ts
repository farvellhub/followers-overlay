declare type Config = {
    TIME_UPDATE: number,
    NUM_FOLLOWERS: string
}

// Personalizar variables
const config: Config = {
    "TIME_UPDATE": 15000,
    "NUM_FOLLOWERS": "30"
}

export default config;